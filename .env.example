# PostgreSQL 连接串示例
# 复制本文件为 .env 并填入你的数据库凭据（.env 已被 .gitignore 排除，不会提交）
# db.ts 在连接串缺少 database 名时会自动回退为 postgres
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=verify-full

# ---------- WebRTC TURN 中继（跨网络/手机流量直连必需） ----------
# 同一 WiFi 下 STUN 即可直连；但手机用流量时处于运营商 CGNAT（对称型 NAT），
# STUN 无法穿透，必须由 TURN 服务器中继媒体。不配置则跨网络无法建立连接。
# 后端 signaling.ts 会读取以下变量并下发给浏览器（非中文环境也会一并下发）。
# 国内移动网络建议用 TURN over TLS（turns:），端口 5349，穿透性最好。
# 三种 url 写法：
#   turn:host:3478          （明文 UDP/TCP，部分运营商会丢 UDP）
#   turns:host:5349         （TLS，推荐）
#   turn:host:3478?transport=tcp
TURN_URL=turn:your-turn-host:3478
# 长期凭证（与 coturn 的 lt-cred-mech 账号密码一致）
TURN_USERNAME=wal
TURN_CREDENTIAL=请替换为强密码

